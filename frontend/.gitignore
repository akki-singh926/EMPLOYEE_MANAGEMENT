# -----------------------------------------------
# 1. DEPENDENCIES & BUILD ARTIFACTS (CRITICAL)
# -----------------------------------------------

# Node Modules (Ignore in both frontend and backend directories)
/node_modules
/backend/node_modules
/frontend/node_modules

# Build Output Directories
/build
/dist
/frontend/build

# Package lock files generated by npm/yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json  # Note: Keep package-lock.json to track dependencies precisely
# If you are NOT using package-lock.json (not recommended), you can ignore it here.


# -----------------------------------------------
# 2. ENVIRONMENT & SECRETS (MANDATORY)
# -----------------------------------------------

# Environment variables (Always ignore these for security)
.env
.env.*
/.env
/backend/.env
/backend/.env.production
/backend/.env.development
/frontend/.env
/frontend/.env.local


# -----------------------------------------------
# 3. UPLOADED FILES (CRITICAL)
# -----------------------------------------------

# Ignore the directory where user-uploaded documents are stored
/backend/uploads/


# -----------------------------------------------
# 4. LOGS, CACHE & MISC
# -----------------------------------------------

# Logs
/logs
*.log

# Operating System Files
.DS_Store
Thumbs.db

# VS Code Metadata (Optional, but usually clean)
.vscode/

# Ignore project files created during local setup
.pnp.js